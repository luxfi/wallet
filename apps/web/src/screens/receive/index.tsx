/**
 * Receive screen — Foundation placeholder.
 *
 * Owned by Send-Receive Blue. Replace with SCREENS.md §2 Receive flow
 * (QR + payment-request URI builder).
 */
export default function Receive(): React.JSX.Element {
  return (
    <section>
      <h1 style={{ fontSize: 24, marginBottom: 8 }}>Receive</h1>
      <p style={{ color: "var(--neutral2, #888)" }}>Send-Receive Blue: replace this file with the QR flow.</p>
    </section>
  )
}
