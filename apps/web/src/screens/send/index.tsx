/**
 * Send screen — Foundation placeholder.
 *
 * Owned by Send-Receive Blue. Replace with SCREENS.md §2 Send flow.
 */
export default function Send(): React.JSX.Element {
  return (
    <section>
      <h1 style={{ fontSize: 24, marginBottom: 8 }}>Send</h1>
      <p style={{ color: "var(--neutral2, #888)" }}>Send-Receive Blue: replace this file with the real flow.</p>
    </section>
  )
}
